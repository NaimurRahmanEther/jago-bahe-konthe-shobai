# Copy to .env for Docker hosting; set both secrets before starting.
POSTGRES_USER=jago
POSTGRES_DB=jago
POSTGRES_PASSWORD=
JWT_SECRET=
# ALLOWED_ORIGINS=https://your-project.vercel.app

JWT_TTL=24h
VALIDITY_THRESHOLD=5
RESPONSE_DEADLINE=168h
BLOCKER_REVIEW_WINDOW=72h
